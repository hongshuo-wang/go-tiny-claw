# 仓库指南

## 项目定位
- 本项目是一个用 Go 编写的、面向 Harness 架构学习的 Agent 原型仓库。
- 仓库目标不只是“实现功能”，还包括帮助贡献者同时学习 Go 基础、Go 工程组织方式，以及 Harness/Agent 的核心设计思路。
- 代码中可能包含基于极客时间课程整理、改写或手动粘贴的示例；在修改前，应先理解意图，再给出实现或讲解。

## 项目结构与模块组织
- CLI 入口位于 `cmd/claw/main.go`，应保持精简，主要负责启动流程、依赖初始化和模块组装。
- 模块定义位于 `go.mod`。根目录 `README.md` 可用于补充学习笔记、架构说明和运行方式。
- 随着代码增加，建议将可复用逻辑拆到 `internal/` 下，例如 `internal/engine/`、`internal/provider/`、`internal/tools/`、`internal/context/`。
- `cmd/` 放入口程序，`internal/` 放内部实现；避免把所有逻辑长期堆积在 `main.go` 中。

## 构建、测试与开发命令
- `go run ./cmd/claw`：本地运行当前 CLI。
- `go build ./cmd/claw`：编译程序，检查是否存在编译错误。
- `go test ./...`：运行整个模块的测试。
- `gofmt -w <文件>`：格式化 Go 源码；修改后应执行。
- `go vet ./...`：进行基础静态检查，适合在提交前运行。

## 代码风格与命名约定
- 遵循标准 Go 风格，以 `gofmt` 为准，不手写特殊格式。
- 包名使用全小写，不使用下划线；导出标识符使用 `PascalCase`，内部函数和变量使用 `camelCase`。
- 文件名应体现职责，例如 `runner.go`、`provider.go`、`registry.go`。
- 新增代码时，优先写清晰、易学、职责单一的实现，不为了“高级感”引入初学者难以理解的复杂抽象。

## 测试规范
- 当前协作偏好：除非用户明确要求，否则不要主动新增 Go 测试用例，也不要创建新的 `*_test.go` 文件；验证时优先使用 `go build`、`go vet`、`go run` 或运行已有测试。
- 测试文件与源码放在同级目录，命名为 `*_test.go`。
- 优先使用表驱动测试，便于学习 Go 测试写法。
- 关键流程应覆盖成功路径和失败路径，尤其是工具调用、上下文管理和 Agent 执行链路。
- 当前没有硬性覆盖率要求，但至少应保证 `go test ./...` 可通过。

## 提交与 Pull Request 规范
- 提交信息建议简洁、明确、使用祈使句，例如 `feat: add provider bootstrap`。
- 每次提交尽量只做一件事，便于学习、复盘和回滚。
- PR 应说明改动目的、涉及模块、运行过的命令，以及是否影响 CLI 输出或 Agent 行为。
- 不要提交构建产物、缓存文件、密钥信息或无关的 IDE 噪音改动。

## 学习型协作说明
- 回答问题时，不仅解释 Harness/Agent 架构，也要同时解释相关 Go 知识，包括语法、类型、包结构、接口、错误处理、项目组织和常见惯用法。
- 默认把仓库维护者视为 Go 初学者：解释应尽量直白，必要时补充“这段 Go 语法为什么这样写”。
- 解释任何 Go 语法知识点时，都要主动和用户熟悉的 Java 进行类比；每次涉及 Go 的 interface、struct、slice、pointer、package、error、goroutine 等概念，都应说明它与 Java 中相近概念的相同点和关键差异。
- 如果用户贴来课程代码或片段，先解释代码在 Go 和 Harness 两个层面的作用，再讨论如何改进、拆分或调试。
- 除回答用户显式提出的问题外，也可以主动指出上下文里值得学习的 Go 知识点、隐藏约定和工程实践，但要保持聚焦，不要无关扩展。
- 讲解代码时，优先按“整体职责 → 关键结构 → 逐行逻辑 → 可改进点”的顺序说明，帮助学习者同时理解设计和语法。
- 当出现 Go 特有概念（如 interface、slice、defer、pointer、goroutine）时，应结合当前代码给出简短解释，而不是只给结论。
